package ru.adiaphora.platform.common.web;

/** Shared API path constants so the version prefix is defined in exactly one place. */
public final class ApiPaths {

    public static final String API_V1 = "/api/v1";

    private ApiPaths() {
    }
}
