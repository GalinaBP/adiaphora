/**
 * Public API of the {@code auth} module: the user directory and the login/registration domain events.
 * Exposed as a Spring Modulith named interface so other modules may depend on it (and only it).
 */
@org.springframework.modulith.NamedInterface("api")
package ru.adiaphora.platform.auth.api;
