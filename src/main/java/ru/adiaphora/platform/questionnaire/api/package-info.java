/**
 * Public API of the {@code questionnaire} module: the answer snapshot and its query service (consumed
 * by {@code rules}) and the answer-updated event (consumed by {@code audit}). Exposed as a Spring
 * Modulith named interface.
 */
@org.springframework.modulith.NamedInterface("api")
package ru.adiaphora.platform.questionnaire.api;
